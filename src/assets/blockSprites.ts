// Block sprites fill the full 16×16 frame — no background stripping needed.
import block from '../sprites/placeholder/block.png'

export const BLOCK_SPRITE_URLS: Record<string, string> = {
  block,
}
